%h2.text-2xl.font-bold.mb-6.text-center Forgot your password?

.flex.items-center.justify-center.bg-gray-100
  .w-full.max-w-md.bg-white.p-8.rounded-lg.shadow-md.flex.flex-col.gap-4
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      = render "devise/shared/error_messages", resource: resource

      .field
        = f.label :email, class: "block text-sm font-medium text-gray-700 mb-2"
        = f.email_field :email, autofocus: true, autocomplete: "email",
          class: "w-full p-2 border border-gray-300 rounded-md shadow-sm focus:border-indigo-500 focus:ring-indigo-500"

      .actions
        = f.submit "Send me reset password instructions",
          class: "w-full py-3 px-4 bg-indigo-600 text-white rounded-md shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"

    = render "devise/shared/links"
